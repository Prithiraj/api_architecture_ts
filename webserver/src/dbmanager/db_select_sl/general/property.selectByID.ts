import pool from '../../dbconn';

export async function select_property_by_id(request: any) {
  const values: any[] = [];

  const id = request.path.split('/').splice(-1)[0];
  const created_by = request.decoded.user_id;
  values.push(id);
  values.push(created_by);

  const select_query = {
    text: `SELECT row_to_json(t)
        FROM (
          SELECT property.*, accountIdproperty
          from property 
          LEFT JOIN property accountIdproperty
          ON property.account_id = accountIdproperty.['id']
          where property.['id'] = $1 and property.created_by = $2
        ) t;`,
    values: values,
  };
  try {
    const result = await pool.query(select_query);
    return result.rows[0].row_to_json;
  }
  catch (err: any) {
    throw err;
  }
}
